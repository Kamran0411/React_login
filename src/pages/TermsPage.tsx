import React from "react";
import "./../styles.css"; // ✅ if your styles are global

const TermsPage: React.FC = () => {
  return (
    <div
      style={{
        minHeight: "100vh",
        backgroundColor: "#111827",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#d1d5db",
        fontSize: "1.5rem",
        textAlign: "center",
        padding: "2rem",
      }}
    >
      This page is in under construction 🏗️
    </div>
  );
};

export default TermsPage;
